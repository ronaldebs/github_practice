// Clase con Javi

// conjunto de sentencias que vamos a llamar por un nombre en concreto


/*
function wake_up()

function eat()

function go_to_sleep()
*/

/*
function cube_volume (edge) {
    return edge * edge * edge;
};


function circle_area (radius) {
    return (radius**2)*3.14
};


function concatenate (a, b) {
    return a + b
};

var edge = Number(prompt('edge length'));

console.log(cube_volume(edge));
*/

// Ejemplos

/*
function discount(price, discount) {
    var money_discounted = price * (discount/100);
    return price - money_discounted
};

//console.log(discount(164,15));

var price = Number(prompt('Pon el precio del producto'));
var discount1 = Number(prompt('Cuanto es el descuento'));

console.log('El precio final del producto es ' +discount(price, discount1));
*/

/*
function number_characters(text) {
    return text.length;
}

var text = String('Real Madrid es el rey de Europa')
console.log(number_characters(text));
*/

/*
function calc(operation, a, b) {
    if (operation === 'sum') {
        result = a + b;
    }
    else if (operation === 'rest') {
        result = a - b;
    }
    else if (operation === 'mult') {
        result = a * b;
    }
    else if (operation === 'div') {
        result = a / b;
    } else alert("operacion invalida");

    return result;
}

console.log(calc('div',100,2))
*/